import { HighlightCapitalizedPipe } from './highlight-capitalized.pipe';

describe('HighlightCapitalizedPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightCapitalizedPipe();
    expect(pipe).toBeTruthy();
  });
});
